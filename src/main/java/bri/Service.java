package bri;

/**
 * Interface de base pour tous les services BRi
 * Chaque service doit implémenter cette interface
 */
public interface Service extends Runnable {
    // Rien à ajouter ici - les services doivent juste implémenter run()
}
